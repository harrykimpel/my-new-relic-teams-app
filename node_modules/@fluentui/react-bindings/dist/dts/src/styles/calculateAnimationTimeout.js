"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var convertCssTimeToNumber_1 = require("./convertCssTimeToNumber");
var _ = require("lodash");
var calculateAnimationTimeout = function (duration, delay) {
    if (_.isNil(duration) || duration === 'inherit' || duration === 'initial')
        return 0;
    if (delay === 'inherit' || delay === 'initial')
        return 0;
    return convertCssTimeToNumber_1.default(duration) + convertCssTimeToNumber_1.default(delay);
};
exports.default = calculateAnimationTimeout;
