"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var _ = require("lodash");
var convertCssTimeToNumber = function (time) {
    if (_.isNil(time) || time === 'initial' || time === 'inherit') {
        return 0;
    }
    if (!_.endsWith(time, 'ms') && !_.endsWith(time, 's')) {
        return 0;
    }
    var multiplier = _.endsWith(time, 'ms') ? 1 : 1000;
    var stringNumber = _.endsWith(time, 'ms') ? time.substring(0, time.length - 2) : time.substring(0, time.length - 1);
    return isNaN(_.toNumber(stringNumber)) ? 0 : multiplier * _.toNumber(stringNumber);
};
exports.default = convertCssTimeToNumber;
