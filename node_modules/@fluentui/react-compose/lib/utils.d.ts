import * as React from 'react';
import { ComposedComponent, ComposeOptions, ComposePreparedOptions, Input } from './types';
export declare const defaultComposeOptions: ComposePreparedOptions;
export declare function mergeComposeOptions(input: Input, inputOptions: ComposeOptions, parentOptions?: ComposePreparedOptions): ComposePreparedOptions;
/**
 * compose() allows you to pass two inputs:
 * - React.forwardRef + static fluentComposeConfig, i.e. previously composed component
 * - a function
 */
export declare function wasComposedPreviously<T extends React.ElementType = 'div', P = {}>(input: Input<T, P>): input is ComposedComponent<P>;
