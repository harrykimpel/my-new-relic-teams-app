import { Accessibility } from '@fluentui/accessibility';
import { ComponentWithAs } from '@fluentui/react-bindings';
import { ChildrenComponentProps, ContentComponentProps, UIComponentProps, ShorthandFactory, ShorthandConfig } from '../../utils';
export interface ToolbarDividerProps extends UIComponentProps, ChildrenComponentProps, ContentComponentProps {
    /**
     * Accessibility behavior if overridden by the user.
     */
    accessibility?: Accessibility<never>;
}
export declare type ToolbarDividerStylesProps = never;
export declare const toolbarDividerClassName = "ui-toolbar__divider";
/**
 * A ToolbarDivider is a non-actionable element that visually segments Toolbar items.
 */
declare const ToolbarDivider: ComponentWithAs<"div", ToolbarDividerProps> & {
    create: ShorthandFactory<ToolbarDividerProps>;
    shorthandConfig: ShorthandConfig<ToolbarDividerProps>;
};
export default ToolbarDivider;
