"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var tslib_1 = require("tslib");
var noop = function () { return undefined; };
var original = tslib_1.__assign({}, console);
var disabled = { log: noop, error: noop, debug: noop, warn: noop, info: noop };
var isDisabledOnce;
/**
 * Enable console logging.
 */
var enable = function () { return Object.assign(console, original); };
/**
 * Disable console logging.
 */
var disable = function () { return Object.assign(console, disabled); };
/**
 * Silence the console for a single test.  It will be re-enabled after it().
 */
var disableOnce = function () {
    isDisabledOnce = true;
    disable();
};
afterEach(function () {
    if (isDisabledOnce) {
        isDisabledOnce = false;
        enable();
    }
});
exports.default = {
    enable: enable,
    disable: disable,
    disableOnce: disableOnce,
};
