import { Accessibility } from '../../types';
/**
 * @specification
 * Adds role 'application' to 'root' slot
 */
declare const videoBehavior: Accessibility;
export default videoBehavior;
export declare type VideoBehaviorProps = never;
