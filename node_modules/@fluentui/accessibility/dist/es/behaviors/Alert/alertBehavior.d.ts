import { Accessibility } from '../../types';
/**
 * @description
 * Uses `alertWarningBehavior` for 'danger' and 'warning' variants.
 */
declare const alertBehavior: Accessibility<AlertBehaviorProps>;
export default alertBehavior;
export declare type AlertBehaviorProps = {
    warning?: boolean;
    danger?: boolean;
    bodyId?: string;
};
